# Optional server-side configuration. You can also enter keys in the UI.
# Leave unused keys blank. Never commit your .env file.
OPENROUTER_API_KEY=
TYPESAFE_API_KEY=
# Use an OpenRouter model ID with tool-calling support.
OPENROUTER_MODEL=openai/gpt-4.1-mini
TYPESAFE_MODEL=jev-latest
PORT=3000
